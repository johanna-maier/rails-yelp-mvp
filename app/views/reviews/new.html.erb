<%= simple_form_for [ @restaurant, @review ] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, input_html: { type: 'number' } %>
  <!-- type number added when params permission did not work but unnecessary -->

  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
